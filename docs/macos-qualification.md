# macOS qualification record

## Current status

**PARTIAL — not macOS-qualified.**

The installed `/Applications/OpenUse.app` has been launched on the arm64 development Mac with its embedded Swift controller. A real user-run Calculator task completed visibly through the installed app. This is useful smoke-test evidence, but it is not a qualification result: TextEdit 3/3, Calculator 3/3, Save As 3/3, Stop, permission-matrix, vision-fallback, DPI, and multi-monitor checks have not all been completed.

Do not describe OpenUse as macOS-qualified until the authoritative results generated by `pnpm qualify:macos` meet the documented acceptance criteria.

## Packaging pass status

- Exact source `app-logo/logo.png`: present and used unchanged by the icon-generation step.
- `pnpm dist:macos`: PASS for the arm64 application and DMG.
- Packaged app: `dist/macos/OpenUse.app`.
- Packaged DMG: `dist/macos/OpenUse-0.1.0-arm64.dmg`.
- Embedded controller: `OpenUse.app/Contents/MacOS/OpenUseMacController`.
- Installed target: `/Applications/OpenUse.app`, copied from the generated DMG.
- Controller IPC: connected during installed-app smoke testing.
- Local signing: ad-hoc; public distribution signing and notarization are not complete.

The ad-hoc signature is sufficient for local installation, but rebuilding can change the code identity macOS uses for privacy permissions. If a rebuilt app asks for Accessibility or Screen Recording again, remove/re-add the installed OpenUse entry or reset only the `com.openuse.app` TCC records, then grant access to the current `/Applications/OpenUse.app`.

## Environment recorded during the packaging pass

```text
macOS             26.0.1
Architecture      arm64
Node              24.18.0
pnpm              10.14.0
Swift             6.2
Monitor count     1
Display bounds    2560 × 1440 points
Scale factor      1× reported by CoreGraphics on this display
```

## Automated evidence

Passing checks from the implementation pass:

- Swift release build.
- Native protocol assertions.
- macOS sidecar JSON-lines smoke test for malformed input, unknown method, enumeration response, invalid window handling, wait, cancellation, reuse, and graceful shutdown.
- TypeScript lint, typecheck, unit tests, and Electron production build.
- Packaged-path and embedded-controller checks.
- Ad-hoc signing and deep signature verification for the packaged app and helper.

## Real scenario evidence

- Calculator: one visibly completed run in the installed app; not counted as a repeatable qualification result.
- TextEdit typing: no 3/3 result recorded.
- TextEdit Save As: no 3/3 result recorded.

The interactive harness writes the authoritative per-attempt metrics to `.openuse/qualification/<run>/results.json` and `report.md`. Screenshots are not stored by default.

## Remaining qualification work

- TextEdit typing: three real AI runs.
- Calculator: three real AI runs with visible `703` verification.
- TextEdit Save As: three real AI runs with visible save-state verification.
- Stop/cancellation twice, including immediate follow-up task.
- Allow Once, Always Allow, Deny, and approval-cancellation checks.
- Accessibility-native versus coordinate fallback counts.
- Vision fallback with screenshot-to-desktop coordinate evidence.
- Retina/scaling checks at the available display settings.
- Multi-monitor check, or an explicit `NOT TESTED` record when only one monitor is available.

Run `pnpm qualify:macos` only against the installed application after the Gateway key and required macOS permissions are configured.
