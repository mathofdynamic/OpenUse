# Qualification scenarios

These JSON files define goals and acceptance criteria only. They deliberately do not contain click coordinates, element IDs, or an application-specific action sequence. The normal OpenUse agent must solve each task through its typed tools and permission layer.

`pnpm qualify:windows` runs each scenario three times in an interactive Windows desktop session and records operator-confirmed results with the redacted runtime event stream.
